query getTariffsToSelector {
    tariffs(
        pagination: {
            pageSize: 100
        }
    ) {
        data {
            id
            attributes {
                name
            }
        }
    }
}
